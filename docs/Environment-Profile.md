# Environment Profile

`.playbook/environment-profile.yml` is authoritative for topology, OS, shell, build/test/start/
cleanup commands, URLs, database access, browser endpoint, logs, migration tool and secret
sources. Replace every placeholder before running commands. Prompts must not invent defaults.
